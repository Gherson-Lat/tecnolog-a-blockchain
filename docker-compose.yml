version: "3.9"

services:
  app:
    build:
      context: .
    container_name: bottle-traceability
    ports:
      - "8001:8001"
    environment:
      PYTHONUNBUFFERED: "1"
    volumes:
      - .:/app
    command: python -m uvicorn app.main:app --host 0.0.0.0 --port 8001
